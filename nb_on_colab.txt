## install kaggle
%%capture
!pip install kaggle

import os
import json

if not os.path.exists('/content/gdrive'):
    from google.colab import drive
    drive.mount("/content/gdrive")


with open('/content/gdrive/MyDrive/Colab Notebooks/_auth/kaggle.json') as json_file:
    data = json.load(json_file)
    os.environ['KAGGLE_USERNAME'] = data["username"]
    os.environ['KAGGLE_KEY'] = data["key"]


## download the dataset
!mkdir data
!cd data && kaggle datasets download -d arroqc/siic-isic-224x224-images && unzip siic-isic-224x224-images.zip
!cd /content/data && wget --no-check-certificate --content-disposition https://github.com/SaschaMet/melanoma-classification/blob/master/data/test.csv
!cd /content/data && wget --no-check-certificate --content-disposition https://github.com/SaschaMet/melanoma-classification/blob/master/data/train.csv